getLevelchartoption() {
    let timer =5
    if(this.eventNum.length == 0)
    timer -
    if(timer ==0 )return
    setTimeout( handler:()=> {
    this.getLevelchartoption()}，timeout: 500)
    return
}